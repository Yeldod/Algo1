#include <iostream>
#include <cmath>

using namespace std; 

int main() {
    double a;
    cin >> a;
    double b;
    cin >> b;
    double c;
    c = sqrt((b * b) + (a * a));
    cout << c << endl;
    return 0;
}